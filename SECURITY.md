# Security Policy

This repository is an editor configuration that installs and executes third-party Neovim plugins. Review `lazy-lock.json` and `install.sh` before installation, especially on managed machines.

Do not report suspected vulnerabilities with secrets or exploit details in a public issue. Use GitHub's **Report a vulnerability** flow under the repository Security tab. For ordinary bugs without sensitive details, open a regular issue.
